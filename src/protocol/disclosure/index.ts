/**
 * Note disclosure — proving what a note holds without any power to spend it.
 * The `orbdisc:` share/verify pair, for auditors and quest verifiers.
 */
export { createNoteDisclosureKey, decodeNoteDisclosureKey } from './noteDisclosure';
export type { NoteDisclosure, NoteDisclosureInput } from './noteDisclosure';
